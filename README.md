# Java BouncyCastle Implementation
This was an old project from back in highschool.
Should generate a self-signed certificate, then establish a secure connection (TLS 1.2) between the Client and Server.
